interface:
  display_name: "SMEDC Delivery Ledger"
  short_description: "SMEDC receipt ledger CSV and certificate photos"
  default_prompt: "Use $smedc-delivery-ledger to query receipt-aware delivery ledger rows, export or maintain per-store monthly CSV files, or manage receipt-linked quarantine-certificate photos."
  brand_color: "#0F766E"
